/**
 * Build-time backend type configuration
 * This file acts as a placeholder and will be replaced during build via angular.json fileReplacements.
 * Both flags are false to ensure this file is never used directly at runtime.
 * If you see both flags as false at runtime, check your build configuration in angular.json.
 */
export const IS_BACKEND_BUILD = false;
export const IS_EDGE_BUILD = false;
